{
  "name": "opui-css",
  "version": "2.0.1",
  "private": false,
  "license": "MIT",
  "files": [
    "dist",
    "src"
  ],
  "sideEffects": [
    "**/*.css"
  ],
  "style": "dist/ui.css",
  "exports": {
    ".": "./dist/ui.css",
    "./dist/*": "./dist/*",
    "./src/*": "./src/*"
  },
  "scripts": {
    "build:css": "pnpm run clean && pnpm exec shx mkdir -p dist && postcss src/main.css -o dist/op+ui.css && postcss src/opui.css -o dist/ui.css",
    "clean": "pnpm exec shx rm -rf dist",
    "dev:css": "pnpm run clean && pnpm exec shx mkdir -p dist && postcss src/main.css -o dist/op+ui.css -w & postcss src/opui.css -o dist/ui.css -w",
    "build": "pnpm run build:css",
    "prepack": "pnpm run build",
    "prepublishOnly": "pnpm run build"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "open-props": "2.0.0-beta.5",
    "postcss-cli": "^11.0.1",
    "postcss-import": "^16.1.1",
    "postcss": "^8.5.6",
    "shx": "^0.4.0"
  }
}
