---
import { prefixClasses as pfx } from "../utils";
type Props = {
  [key: string]: any
  class?: string
}

const { class: className, ...rest } = Astro.props
---

<dd class:list={pfx(["description", className])} {...rest}>
  <slot />
</dd>
