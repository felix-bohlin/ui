---
import { prefix } from "../../css/js/prefix.js"
type Props = {
  [key: string]: any
  class?: string
}

const { class: className, ...rest } = Astro.props
---

<dd class:list={[prefix("description"), className]} {...rest}>
  <slot />
</dd>
