---
import { prefix } from "../../css/js/prefix.js"
type Props = {
  [key: string]: any
  class?: string
}

const { class: className, ...rest } = Astro.props
---

<div class:list={[prefix("item"), className]} {...rest}>
  <slot />
</div>
