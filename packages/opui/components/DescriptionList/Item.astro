---
import { prefixClasses as pfx } from "../utils";
type Props = {
  [key: string]: any
  class?: string
}

const { class: className, ...rest } = Astro.props
---

<div class:list={pfx(["item", className])} {...rest}>
  <slot />
</div>
