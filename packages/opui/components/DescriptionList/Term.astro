---
import { prefix } from "../../css/js/prefix.js"
type Props = {
  [key: string]: any
  class?: string
}

const { class: className, ...rest } = Astro.props
---

<dt class:list={prefix("term", className)} {...rest}>
  <slot />
</dt>
