---
import { prefixClasses as pfx } from "../utils";
type Props = {
  [key: string]: any
  class?: string
}

const { class: className, ...rest } = Astro.props
---

<dt class:list={pfx(["term", className])} {...rest}>
  <slot />
</dt>
