---
import { prefix } from "../../css/js/prefix"
type Props = {
  [key: string]: any
  class?: string
}

export const title = "Drawer Footer"

const { class: className, ...rest } = Astro.props
---

<div class:list={[prefix("footer"), className]} {...rest}>
  <slot />
</div>
