---
import type { Props } from "./types"

export const title = "Drawer Footer"

const { class: className, ...rest } = Astro.props
---

<div class:list={["footer", className]} {...rest}>
  <slot />
</div>
