---
import type { DrawerFooterProps as Props } from "./types"

export const title = "Drawer Footer"

const { class: className, ...rest } = Astro.props
---

<div class:list={["ui-footer", className]} {...rest}>
  <slot />
</div>
