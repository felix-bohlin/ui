---
import type { Props } from "./types.astro"

export const title = "Field Description"

const { class: className, ...rest } = Astro.props
---

<!-- prettier-ignore -->
<p class:list={["field-description", className]} {...rest}><slot /></p>
