---
import { prefix } from "../../css/js/prefix.js"
type Props = {
  [key: string]: any
  class?: string
}

export const title = "Field Description"

const { class: className, ...rest } = Astro.props
---

<!-- prettier-ignore -->
<p class:list={[prefix("field-description"), className]} {...rest}><slot /></p>
