---
import type { Props } from "./types.astro"

export const title = "Field Set"

const { as: Tag = "fieldset", class: className, ...rest } = Astro.props
---

<Tag class:list={["fieldset", className]} {...rest}>
  <slot />
</Tag>
