---
import type { FormProps as Props } from "./types.ts"

export const title = "Form"

const { as: Tag = "form", class: className, ...rest } = Astro.props
---

<Tag class:list={["form", className]} {...rest}>
  <slot />
</Tag>
