---
import type { RadioInputProps as Props } from "./types.ts"

const { checked, disabled, id, name, required, value, ...rest } = Astro.props
---

<input
  type="radio"
  {checked}
  {disabled}
  {id}
  {name}
  {required}
  {value}
  {...rest}
/>
