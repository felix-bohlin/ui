---
import type { SwitchInputProps as Props } from "./types.ts"

const { checked, disabled, id, name, required, value, ...rest } = Astro.props
---

<input
  type="checkbox"
  role="switch"
  {checked}
  {disabled}
  {id}
  {name}
  {required}
  {value}
  {...rest}
/>
