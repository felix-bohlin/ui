---
import type { ColumnProps as Props } from "./types"

export const title = "Table Column"

const { width, ...rest } = Astro.props
---

<col
  style={width ? (width.includes(":") ? width : `width: ${width}`) : undefined}
  {...rest}
/>
