---
import type { Props } from "./types"

export const title = "Table"

const { class: className, variant, ...rest } = Astro.props
---

<table class:list={[className, variant]} {...rest}>
  <slot />
</table>
