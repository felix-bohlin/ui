---
type Props = {
  [key: string]: any
  class?: string
  variant?: "dense" | "spacious"
}

export const title = "Table"

const { class: className, variant, ...rest } = Astro.props
---

<table
  class:list={["ui-table", className, variant && `ui-${variant}`]}
  {...rest}
>
  <slot />
</table>
