---
import type { TabsTabProps as Props } from "./types.astro"

const { class: className, tabId, ...rest } = Astro.props
const { currentTabId } = Astro.locals
---

<label
  for={tabId || currentTabId}
  class:list={["ui-tab-label", className]}
  role="tab"
  {...rest}
>
  <slot />
</label>
